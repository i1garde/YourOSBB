@model List<YourOSBB.Web.Models.Entities.ComplaintViewModel>

@{
    ViewBag.Title = "title";
    Layout = "_ResidentLayout";
}

<div>
    <a asp-controller="Complaint" asp-action="CreateComplaint" class="btn btn-secondary">
        Створити скаргу
    </a>
</div>
<div>
    <h3>Скарги від жителів ОСББ:</h3>
    @foreach (var obj in Model)
    {
        <tr>
            <td>
                @obj.Name
            </td>
            <br/>
            <td>
                @obj.Text
            </td>
            <br/>
            <td>
                @obj.Date
            </td>
            <br/>
        </tr>
        <br/>
    }
</div>